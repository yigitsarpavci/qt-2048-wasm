name: Deploy 2048 to GitHub Pages
on:
  push:
    branches: [ main ]
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Cache Qt
        id: cache-qt
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/Qt
          key: ${{ runner.os }}-qt-6.6.1-wasm-v8
          
      - name: Install Host Qt
        if: steps.cache-qt.outputs.cache-hit != 'true'
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.6.1'
          target: 'desktop'
          arch: 'gcc_64'
          dir: ${{ github.workspace }}
          
      - name: Install WASM Qt
        if: steps.cache-qt.outputs.cache-hit != 'true'
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.6.1'
          target: 'desktop'
          arch: 'wasm_singlethread'
          dir: ${{ github.workspace }}
          
      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v12
        with:
          version: '3.1.37'
          
      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ${{ runner.os }}-ccache
          
      - name: Build Project
        run: |
          mkdir -p build_clean
          cd build_clean
          
          # Absolute Path Discovery with Permission Hardening
          QT_ROOT="${{ github.workspace }}/Qt/6.6.1"
          QT_WASM_PATH=$(find $QT_ROOT -name "wasm_singlethread" | head -n 1)
          QT_HOST_PATH=$(find $QT_ROOT -name "gcc_64" | head -n 1)
          
          if [ -z "$QT_WASM_PATH" ]; then echo "Error: WASM Qt not found"; exit 1; fi
          
          # Fix permissions for CACHED binaries
          chmod +x $QT_WASM_PATH/bin/* || true
          chmod +x $QT_HOST_PATH/bin/* || true
          
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          $QT_WASM_PATH/bin/qt-cmake .. -DQT_HOST_PATH=$QT_HOST_PATH -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache
          cmake --build . --parallel $(nproc)
          
      - name: Prepare Deployment
        run: |
          mkdir -p public
          rm -rf public/*
          cp -r build_clean/* public/
          cp src/style.css public/
          mv public/2048.html public/index.html || true
          
          if [ ! -f "public/index.html" ]; then echo "Error: index.html missing"; exit 1; fi
          
          SHA_SHORT=$(echo ${{ github.sha }} | cut -c1-7)
          sed -i "s/\([a-zA-Z0-9]*\)\.js/\1\.js?v=${SHA_SHORT}/g" public/index.html
          sed -i "s/2048\.wasm/2048\.wasm?v=${SHA_SHORT}/g" public/index.html

      - name: Refactor HTML for Mobile Layout
        run: |
          python3 - <<'EOF'
          import os, re
          path = 'public/index.html'
          if not os.path.exists(path):
              print("index.html not found")
              exit(1)
              
          with open(path, 'r') as f:
              content = f.read()

          # 1. Inject Stylesheet
          head_split = content.split('</head>')
          if len(head_split) > 1:
              new_head = head_split[0] + '<link rel="stylesheet" href="style.css">\n</head>'
              content = new_head + head_split[1]

          # 2. Wrap Canvas/Container
          # Qt usually puts things in a div or body. We wrap the main interactive part.
          # We look for the canvas or the default container.
          if '<div id="qt-container">' in content:
              # Already has a container, wrap it
              content = content.replace('<div id="qt-container">', '<div id="game-container"><div id="qt-container">')
              # Find the end of that div and close our wrapper
              # This is simplified; assumes the qt-container is the main thing in body
              content = content.replace('</body>', '</div></div></body>')
          else:
              # No qt-container? Let's wrap the whole body content before scripts
              body_match = re.search(r'<body[^>]*>(.*?)<script', content, re.DOTALL)
              if body_match:
                  inner_body = body_match.group(1)
                  wrapped = f'<div id="game-container"><div id="qt-container">{inner_body}</div></div>'
                  content = content.replace(inner_body, wrapped)

          with open(path, 'w') as f:
              f.write(content)
          print("HTML Refactored for Mobile Layout successfully.")
          EOF


      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
